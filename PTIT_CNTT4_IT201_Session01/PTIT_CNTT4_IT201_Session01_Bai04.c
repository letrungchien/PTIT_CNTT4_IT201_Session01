#include <stdio.h>
int main() {

    // C1
    int sum =0;
    for (int i=1;i<=n;i++) {
        sum +=1;
    }
        // Độ phức tạp không gian của  cách 2 là O(n)

    // C2
    int sum =0;
    sum = n(n+1)/2

    // Độ phức tạp không gian của cách 2 là O(1)
    return 0;
}